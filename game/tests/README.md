# Game Tests

Deterministic domain, content-registry, save/migration, offline-time, scene, and presentation tests will be added with their implementations. Prompt 0 validates only the shell and data fixtures.
