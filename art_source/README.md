# Art Source

Store generation briefs/prompts in `prompts/` and structured provenance records in `provenance/`. Original model outputs will use an ignored generated-source location or an explicitly approved large-file strategy. Only reproducibly processed, approved runtime assets enter `game/assets_generated/`.

Do not place secrets, unauthorized third-party material, or reference mockups here. See `docs/AI_ASSET_PIPELINE.md` and `docs/LEGAL_AND_IP_BOUNDARIES.md`.
