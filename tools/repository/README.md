# Repository Checks

`check_markdown_links.py` verifies repository-relative Markdown links and image targets without network access. Other release/packaging checks should be added only when deterministic and relevant.
