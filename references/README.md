# Design References

Reference assets are documentation inputs, not runtime assets. They stay outside the Godot `res://` root and must not be shipped, modified, recompressed, or treated as production art without explicit approval and verified rights.

See `ui-modes/README.md` for expected UI-mode concepts and current availability.
