# Tool Agent Rules

Obey root `AGENTS.md`. Tools must be deterministic, non-interactive for validation, safe on untrusted paths, and explicit about inputs/outputs. Pin dependencies, avoid network/service requirements, emit actionable file and JSON-path errors, and add fixtures/tests with behavior changes. Never overwrite source art or saves in place.
