# Draft Content Schemas

JSON Schema 2020-12 contracts for content API `0.1`. They are deliberately small and expected to evolve through versioned compatibility/migrations. Instances point to their schema with a repository-relative `$schema` path.

Validate bundled and example packs with:

```sh
python3 tools/content_validation/validate_content.py
```
